{
  "mcpServers": {
    "mysql": {
      "command": "cmd",
      "args": [
        "/c",
        "npx",
        "-y",
        "@fhuang/mcp-mysql-server",
        "mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@${MYSQL_HOST}:${MYSQL_PORT}/datax"
      ]
    }
  }
}